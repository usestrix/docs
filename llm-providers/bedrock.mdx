---
title: "AWS Bedrock"
description: "Configure Strix with models via AWS Bedrock"
---

## Setup

```bash
export STRIX_LLM="bedrock/anthropic.claude-3-sonnet-20240229-v1:0"
```

No API key required—uses AWS credentials from environment.

## Authentication

### Option 1: AWS CLI Profile

```bash
export AWS_PROFILE="your-profile"
export AWS_REGION="us-east-1"
```

### Option 2: Access Keys

```bash
export AWS_ACCESS_KEY_ID="AKIA..."
export AWS_SECRET_ACCESS_KEY="..."
export AWS_REGION="us-east-1"
```

### Option 3: IAM Role (EC2/ECS)

Automatically uses instance role credentials.

## Available Models

| Model | Description |
|-------|-------------|
| `bedrock/anthropic.claude-3-sonnet-20240229-v1:0` | Claude Sonnet |
| `bedrock/anthropic.claude-3-opus-20240229-v1:0` | Claude Opus |
| `bedrock/anthropic.claude-3-haiku-20240307-v1:0` | Claude Haiku (fast) |
| `bedrock/amazon.titan-text-premier-v1:0` | Amazon Titan |

## Prerequisites

1. Enable model access in the AWS Bedrock console
2. Ensure your IAM role/user has `bedrock:InvokeModel` permission

